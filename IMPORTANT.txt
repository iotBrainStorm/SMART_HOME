//// IMPORTANT ////
*AI model Claude Opus 4.6 (3x) 




very nice all section looking very nice, working very smoothly, we need to modify and add somethings like

fix these slight issues
=> every time when i backed from settings it shows login popup. while user totally backed from home screen then login will be required. also show "save password" popup which is in generally occurs to save password for smooth further login
=> Fix dropdown text color, now it is difficult to understand due to same background color 
=> Set menu tabs at horizontally center
=> Set a fix width of all menu buttons about 110px and align the text inside this menu button to center
=> Set a margin about 30px at top of the config body for good looking
 


now add section
=> At switch menu add one more submenu "Relay Holding State". inside this "Relay off at startup (Default)" "Output off at startup" "Output on at startup" "Remember last state of output". these options will be availabe for every switches not common case
=> while clicked to change switch icon then shows some default icons of devices and room
=> At Set Schedule section set a condition such as user can set upto 6 schedules for each switches. if possible then also check the conditions by user are valid or not, means any condition should not be overlapping
=> Do same thing for Future Schedule
=> At IP Settings => DHCP on/off toggle, if DHCH off then Static configaration will appear to edit "Static IP" "Subnet Mask" "Default Gateway" "DNS" "MAC Address(Only read)" 
=> At firebase secion add one more submenu "on/off" toggle like bluetooth. also add  "Database Rules(Description box)" to take json code
=> 


*** all css, js will in one page, no separate file




